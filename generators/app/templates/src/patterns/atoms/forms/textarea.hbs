{{!--
class: custom class
placeholder: placeholder text
required: if the field is going to be required and will use the abide validation [TRUE] is the value
tabindex: the tab index order number
textarea-title: title of the textarea
--}}

<textarea{{#if class}} class="{{class}}"{{/if}}{{#if placeholder}} placeholder="{{placeholder}}"{{/if}}{{#if tabindex}} tabindex="{{tabindex}}"{{/if}}{{#if textarea-title}} title="{{{textarea-title}}}"{{/if}}{{#if required}} required{{/if}}></textarea>